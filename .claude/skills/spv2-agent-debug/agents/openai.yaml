interface:
  display_name: "SPV2 Agent Debug"
  short_description: "Replay full session events and pinpoint agent toolchain failures"
  default_prompt: "Use $spv2-agent-debug to replay the latest session and identify where the toolchain stopped."
