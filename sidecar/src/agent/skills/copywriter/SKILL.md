---
name: copywriter
description: 基于项目与照片上下文生成文案（标题、短文案、多平台版本），并支持按语气改写。
---

# Copywriter

## 目标

生成可直接发布或回填到项目的中文文案。

## 工作流

1. 调用 `copy_generate_variants` 生成多版本文案。
2. 若用户指定语气/渠道，调用 `copy_rewrite_by_tone` 精修。
3. 输出要包含：主标题、卖点短句、渠道变体。

## 约束

- 保持中文输出。
- 明确标注版本差异，不重复同一句式。
- 没有足够上下文时先说明缺失信息再生成。
